describe('useAuthentication hook', () => {
  it('should log out because token is invalid', () => {

  });

  it('should invalidate token with refresh token because token is invalid', () => {

  });

  it('should log out because token and refresh token is invalid', () => {

  });
});
